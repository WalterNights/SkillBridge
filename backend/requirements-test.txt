-r requirements-prod.txt

pytest==8.3.4
pytest-django==4.9.0
pytest-mock==3.14.0

# Linter + formatter (también lo corre pre-commit con su propio venv;
# acá lo dejamos disponible para los devs que prefieran correrlo a mano).
ruff==0.8.4
pre-commit==4.0.1
