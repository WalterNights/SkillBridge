# Django Settings
SECRET_KEY=your-secret-key-here-generate-a-secure-one
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (SQLite for development)
# For production, use PostgreSQL
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=../database/db.sqlite3

# PostgreSQL Configuration (uncomment for production)
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=skillbridge_db
# DATABASE_USER=skillbridge_user
# DATABASE_PASSWORD=your-secure-password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Celery & Redis Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
REDIS_CACHE_URL=redis://127.0.0.1:6379/1

# Email Configuration
# Para desarrollo, usa console backend (imprime emails en consola)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Para producción, usa SMTP (ejemplo con Gmail):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@skillbridge.com

# Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# Sentry (for production monitoring)
# SENTRY_DSN=your-sentry-dsn
# ENVIRONMENT=development
